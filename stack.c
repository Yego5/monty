#include "monty.h"

/**
 * f_stack - sets the list to stack mode (LIFO)
 * @stk: stack head
 * @l_nmb: line_number
 * Return: no return
 */
void f_stack(stack_t **stk, unsigned int l_nmb)
{
	(void)stk;
	(void)l_nmb;
	bus.lifi = 0;
}
